import argparse

parser = argparse.ArgumentParser(description='Simple configuration for Python applications.')
parser.add_argument('-s', type=str, nargs='+',
                   help='The schema file to configure.')

args = parser.parse_args()
print args